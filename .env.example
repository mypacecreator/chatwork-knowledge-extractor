# Chatwork API設定
CHATWORK_API_TOKEN=your_chatwork_api_token_here
CHATWORK_ROOM_ID=your_room_id_here

# Claude API設定
CLAUDE_API_KEY=your_claude_api_key_here
# 使用モデル（省略時は claude-sonnet-4-5-20250929）
# 例: claude-sonnet-4-5-20250929, claude-haiku-4-5-20251001
# CLAUDE_MODEL=claude-haiku-4-5-20251001

# API種別（省略時は batch）
# - batch: Batch API（50%割引、処理時間: 数分〜24時間）
# - realtime: Realtime API（通常価格、処理時間: 数秒〜数分）
CLAUDE_API_MODE=realtime

# Claude分析の最大トークン数
# JSONパースエラーが発生する場合は増やしてください
# デフォルト: 2000（1つのメッセージから複数の知見を抽出する場合に対応）
# 推奨範囲: 1500-3000
CLAUDE_MAX_TOKENS=2000

# ログ設定
# デバッグモード（省略時は false）
# - false: 通常ログのみ出力（ERROR, WARN, INFO）
# - true: 詳細ログも出力（DEBUG含む）
# DEBUG_MODE=true

# 分析設定
# 分析対象の期間（省略時は全期間）
# - 数字: 過去N日間（例: 90）
# - 日付: 指定日以降（例: 2025-01-01）
EXTRACT_FROM=90
# 分析対象の最大件数（キャッシュから取り出す件数）
MAX_MESSAGES=500

# メッセージフィルタリング設定（Claude API送信前の事前除外）
# 最小文字数（これ未満のメッセージは除外）
FILTER_MIN_LENGTH=10
# 最大文字数（これを超えるメッセージは切り詰め）
FILTER_MAX_LENGTH=300
# 定型文除外の閾値（この文字数以上なら定型文で始まっても通す）
# 例: 「了解です。次回からは〇〇の手順でテストします。」のようなケースを救済
FILTER_BOILERPLATE_THRESHOLD=50

# 出力設定
OUTPUT_DIR=./output

# 出力する汎用性レベル（high, medium, low をカンマ区切り）
# デフォルト: high,medium
# - high のみ: 最も厳格（普遍的な技術知見のみ）
# - high,medium: 推奨（業界特有の知見も含む）
# - high,medium,low: すべて（判断例も含む）
OUTPUT_VERSATILITY=high,medium

# カスタムプロンプトテンプレートのパス（省略時は prompts/analysis.md を使用）
# PROMPT_TEMPLATE_PATH=./prompts/custom.md

# フィードバックファイルのパス（省略時は feedback/corrections.json を使用）
# FEEDBACK_PATH=./feedback/corrections.json

# チームプロファイル設定（発言者ロールによる重み付け分析）
# account_idとロール（senior/member/junior）の対応を定義するJSONファイルのパス
# 省略時は config/team-profiles.json を使用（ファイルがなければ全員member扱い）
# TEAM_PROFILES_PATH=./config/team-profiles.json
